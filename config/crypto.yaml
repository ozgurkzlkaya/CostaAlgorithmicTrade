mode: CRYPTO_FUTURES
symbols:
  universe: ["BTCUSDT", "ETHUSDT"]
  min_volume_usd: 5000000
  min_price: 0.01
  exclude: []
providers:
  market: "ccxt"
  exchange: "binance"
  rate_limit: true
  cache: true
# Note: spread/liquidity/wick filters are not yet implemented. Keep this file for
# exchange and symbol universe overrides; filter configuration will be added when
# the implementations land.
